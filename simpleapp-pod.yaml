apiVersion: v1
kind: Pod
metadata:
  name: simple-app
  labels: 
     name: simple-app
spec:
  containers:
    - name: simple-app-con
      image: simple-app:1.0
      ports: 
        - containerPort: 3000
